# Copy to .env.local to use a real model through server/task-doctor.mjs (npm run ai-proxy).
# Without it, AI Task Doctor uses the local stub.
VITE_TASK_DOCTOR_ENDPOINT=http://127.0.0.1:8787/task-doctor
